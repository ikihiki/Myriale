import { describe, expect, it } from 'vitest';
import { toTurnInspection, type TurnInspectionDto } from './turnInspectionModel';

const dto: TurnInspectionDto = { sessionId: 'SES-1', scenarioId: 'SCN-1', scenarioTitle: '星の物語', turn: { id: 'TURN-1', position: 7, kind: 'narrative', createdAt: '2026-07-29T10:00:04Z' }, execution: { id: 'EXEC-1', status: 'succeeded', startedAt: '2026-07-29T10:00:00Z', completedAt: '2026-07-29T10:00:04Z', elapsedMilliseconds: 4036, timeline: [{ stage: 'rules', durationMilliseconds: 180 }] }, aiInteractions: [{ id: 'later', executionId: 'EXEC-1', executionAttemptId: 'ATT-1', attemptNumber: 1, sequence: 2, stage: 'narrative', aiProfileId: 'p2', status: 'succeeded', startedAt: '2026-07-29T10:00:02Z' }, { id: 'first', executionId: 'EXEC-1', executionAttemptId: 'ATT-1', attemptNumber: 1, sequence: 1, stage: 'decision', aiProfileId: 'p1', status: 'succeeded', provider: 'Provider', model: 'Model', startedAt: '2026-07-29T10:00:00Z', completedAt: '2026-07-29T10:00:00.540Z', latencyMilliseconds: 540, inputTokens: 100, outputTokens: 20, sentPrompt: 'prompt', receivedResult: 'result' }], ruleEngine: { durationMilliseconds: 180, selectedAction: { actionId: 'open' }, preState: { open: false }, postState: { open: true } } };
describe('toTurnInspection', () => {
  it('retains turn identity, sorts AI exchanges, and formats elapsed timing', () => { const value = toTurnInspection(dto); expect(value.turn).toMatchObject({ id: 'TURN-1', position: 7 }); expect(value.execution?.elapsedLabel).toBe('4.04 秒'); expect(value.execution?.timeline[0].durationLabel).toBe('180 ms'); expect(value.interactions.map((item) => item.id)).toEqual(['first', 'later']); });
  it('provides readable placeholders without losing raw JSON', () => { const interaction = toTurnInspection(dto).interactions[1]; expect(interaction).toMatchObject({ providerLabel: '不明', modelLabel: '不明', latencyLabel: '計測なし', sentPromptLabel: '送信プロンプトは記録されていません。' }); expect(toTurnInspection(dto).rawJson).toContain('"TURN-1"'); });
});
