import { createFileRoute } from '@tanstack/react-router'; export const Route = createFileRoute('/evaluations/$id/results')({ component: Page }); function Page() { const { id } = Route.useParams(); const { evaluationResultsContainer: Container } = Route.useRouteContext(); return <Container evaluationId={id}/>; }
