import { createFileRoute } from '@tanstack/react-router'; export const Route = createFileRoute('/evaluations/$id/')({ component: Page }); function Page() { const { id } = Route.useParams(); const { evaluationOverviewContainer: Container } = Route.useRouteContext(); return <Container evaluationId={id}/>; }
